local M = {}

function M.saturate(h, s, l, amt)
    error("unimplemented")
    return h, s, l
end

function M.lighten(h, s, l, amt)
    error("unimplemented")
    return h, s, l
end

function M.warm(h, s, l, amt)
    error("unimplemented")
    return h, s, l
end

return M
